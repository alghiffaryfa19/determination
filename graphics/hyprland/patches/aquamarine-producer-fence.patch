## SPDX-FileCopyrightText: 2026 Aurora contributors
## SPDX-License-Identifier: MIT
##
## Aurora patch for BSD-3-Clause Aquamarine. Retain Aquamarine's
## copyright and BSD-3-Clause notice with this patch and its output.

--- a/src/backend/Headless.cpp
+++ b/src/backend/Headless.cpp
@@ -44,7 +44,7 @@
 
 bool Aquamarine::CHeadlessOutput::commit() {
     if (name == "HWC-1" && state->state().buffer &&
-        !Hyprland::HwcPresenter::instance().present(state->state().buffer)) return false;
+        !Hyprland::HwcPresenter::instance().present(state->state().buffer, state->state().explicitInFence)) return false;
     events.commit.emit();
     state->onCommit();
     needsFrame = false;
